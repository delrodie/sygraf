{% extends 'base.html.twig' %}

{% block body %}
    <h1>Participer</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participer.id }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ participer.annee }}</td>
            </tr>
            <tr>
                <th>Publiepar</th>
                <td>{{ participer.publiePar }}</td>
            </tr>
            <tr>
                <th>Modifiepar</th>
                <td>{{ participer.modifiePar }}</td>
            </tr>
            <tr>
                <th>Publiele</th>
                <td>{{ participer.publieLe }}</td>
            </tr>
            <tr>
                <th>Modifiele</th>
                <td>{{ participer.modifieLe }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('participer_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('participer_edit', { 'id': participer.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
